# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Set up Java
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: "17"
    - name: Install kaitai-struct-compiler
      run: |
        curl -LO https://github.com/kaitai-io/kaitai_struct_compiler/releases/download/0.11/kaitai-struct-compiler_0.11_all.deb
        sudo apt-get update
        sudo apt-get install -y ./kaitai-struct-compiler_0.11_all.deb
    - name: Install sbt
      run: |
        sudo mkdir -p /usr/share/keyrings
        echo "deb [signed-by=/usr/share/keyrings/sbt.gpg] https://repo.scala-sbt.org/scalasbt/debian all main" | sudo tee /etc/apt/sources.list.d/sbt.list
        echo "deb [signed-by=/usr/share/keyrings/sbt.gpg] https://repo.scala-sbt.org/scalasbt/debian /" | sudo tee /etc/apt/sources.list.d/sbt_old.list
        curl -fsSL https://repo.scala-sbt.org/scalasbt/debian/gpg | sudo gpg --batch --yes --dearmor -o /usr/share/keyrings/sbt.gpg
        sudo apt-get update
        sudo apt-get install -y sbt
    - name: Submodule update
      run: |
        git submodule update --init
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install flake8 pytest numpy
    - name: Test
      run: |
        make test
    - name: Build docs
      if: matrix.python-version == '3.11'
      run: |
        python -m pip install .[docs]
        mkdocs build
    - name: Deploy docs
      if: matrix.python-version == '3.11' && github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: |
        mkdocs gh-deploy --force
